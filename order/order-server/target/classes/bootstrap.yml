spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
      label: master
    stream:
      bindings:
        input:
          group: order
          content-type: application/josn
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8761/eureka/


